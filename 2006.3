/*Problem Description
给你n个整数，求他们中所有奇数的乘积。
Input
输入数据包含多个测试实例，每个测试实例占一行，每行的第一个数为n，
表示本组数据一共有n个，接着是n个整数，你可以假设每组数据必定至少存在一个奇数。
Output
输出每组数中的所有奇数的乘积，对于测试实例，输出一行。*/
#include<stdio.h>

int main()
{
    int n,a[100];
    while(scanf("%d",&n)==1)        //输入数字n代表需要输入的整数个数
    {
        int i,m=1;
        for(i=0;i<n;i++)
        {
            scanf("%d",&a[i]);           //用数组来记录这一组数据
        }
        for(i=0;i<n;i++)
        {
            if(a[i]%2!=0)                    //判断奇偶性，做乘积
                m*=a[i];
        }
        printf("%d\n",m);            //输出奇数的乘积
    }
    return 0;
}


//虽然还是显得小题大做，但这确实是一个不错的想法（在需要用到一组需要保留的数据时）
